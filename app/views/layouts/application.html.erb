<!DOCTYPE html>
<html>
  <head>
    <title>VotingGuide</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class = "main">    
  <p style="text-align:right">
    <% if logged_in? %>
      <%= link_to "Logout", sessions_path, method: :delete %>

      <%= button_to "Delete Account", current_user, method: :delete, data: {confirm: "Are you sure?"}%>
 
    <% else %>
      <%= link_to "Sign up", signup_path %> or <%= link_to "sign in", login_path %> to create your own voting guide
    <% end %>
    <% flash.each do |k, v| %>
        <p class="<%= k %>"><%= v %></p>
      <% end %>
      <h1>Ballot Buddy</h1>
      <h2>Fall 2020: San Francisco</h2>
    </p>
    <%= yield %>
  </body>
</html>
</div>